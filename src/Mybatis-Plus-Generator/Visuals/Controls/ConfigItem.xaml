<UserControl x:Class="Mybatis_Plus_Generator.Visuals.Controls.ConfigItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mybatis_Plus_Generator.Visuals.Controls"
             xmlns:abs="clr-namespace:Mybatis_Plus_Generator.Definition.Abstractions;assembly=Mybatis-Plus-Generator.Definition"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance abs:ConfigItemInfo}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBox 
            IsEnabled="{Binding IsEnable}"
            VerticalAlignment="Top" Text="{Binding Value}" 
            Margin="0,0,0,20" FontSize="16"
            md:HintAssist.Hint="{Binding TemplateInfo.FieldName,Converter={StaticResource Lang}}"
            Style="{StaticResource MaterialDesignFloatingHintTextBox}">
        </TextBox>
    </Grid>
</UserControl>
